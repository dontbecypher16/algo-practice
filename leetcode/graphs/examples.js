function breadthFirstTraversal(root) {
    let queue = [root]

    while(queue.length > 0) {
        // get node from queue
        // do something with it
        // load node.children into queue
    }
}

function depthFirstTraversal(root) {
    let stack = [root]

    while (stack.length > 0) {
        // get node from stack
        // do something with it
        // load node.children into stack
    }
}

// stack
// last in first out
// [] <-->


// queue
// first in first out
// <-[]<-